# frontend/

Next.js operator console. Spec: `docs/FRONTEND.md`.

Same-origin only: `/api/bot/*`, `/api/stream?chain=`, `/api/eth`.
Demo data when the engine is down, with a loud **DEMO DATA** banner.

Does not sign solver or sidecar payloads. Does not sandwich. Does not
blend mouths into one APY.
